#include "pch.h"

int main()
{
	//std::unique_ptr<CommunityServer> community_server = std::make_unique<CommunityServer>();
	//if (false == community_server->Initialize())
	//{
	//	Logger::Error("Failed to initialize community_server.");
	//	return -1;
	//}

	//community_server->Start();

	//// Start 안에서 대기 중

	//community_server->Shutdown();
	//community_server->Finalize();

	return 0;
}