include "CommonTable.fbs";

attribute "priority";

namespace fb.world;

enum SendPid : uint32
{
	None = 0,
	Reg,
	Ping,
}


// ######################################################
// recv pid

/// [ENUM] (C<-GS) Recv Packet id
enum RecvPid : uint32
{
	None = 0,
	Reg,
	Ping,
}
